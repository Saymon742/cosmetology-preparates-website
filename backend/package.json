{
  "name": "cosmeticlab-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "seed": "python seed_data.py"
  },
  "dependencies": {
    "nodemon": "^3.0.1"
  }
}